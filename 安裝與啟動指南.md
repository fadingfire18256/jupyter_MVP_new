# 安裝與啟動指南

從一台什麼都沒有的電腦，到可以開始上課。

環境建置大約 **15 分鐘**，照著做就好，不需要程式基礎。

---

## 這堂課要做什麼

你會親手做出一套「電影整合系統」的網頁：

- 自動抓兩家影城（秀泰、美麗華）現在上映什麼
- 補上每部片的評分、海報、類型
- 整合成一份清單，可以依評分、類型、上映日期篩選
- 加上 AI 聊天，讓它根據這份片單回答問題

課程分成六本 Jupyter Notebook，**依序做完就會得到一套能操作的系統**。

而且這包教材拿到手時**只有 notebook** —— 整套系統包含網頁前端，
都是你在課程中一格一格寫出來的。

### 六個步驟

| 步驟 | 做什麼 | 花多久 |
|---|---|---|
| 1 | 安裝 Python | 5 分鐘 |
| 2 | 把教材資料夾放好 | 1 分鐘 |
| 3 | 建立虛擬環境 | 2 分鐘 |
| 4 | 安裝套件 | 3 分鐘 |
| 5 | 啟動 Jupyter | 1 分鐘 |
| 6 | 依序做完六本教材 | 上課時間 |

> **步驟 1 到 4 只做一次。** 之後每次上課，只要做步驟 5。
> 文件後面有「下次上課的時候」的兩行快速指令。

---

# 步驟 1 ・ 安裝 Python

這堂課的程式是用 Python 寫的，Jupyter 本身也是一個 Python 套件。
所以 Python 是所有東西的基礎，要先裝它。Windows 預設沒有內建。

## 1-1 先確認是不是已經有了

打開命令列 —— 按鍵盤的 `Win` + `R`，輸入 `cmd`，按 Enter。

在黑色視窗裡輸入這行，按 Enter：

```
python --version
```

**看到 `Python 3.9.x` 或更高的版本** → 你已經有了，直接跳到步驟 2。

**看到錯誤訊息、或版本低於 3.9** → 繼續往下做。

## 1-2 下載安裝檔

打開瀏覽器，前往 Python 官方網站：

**https://www.python.org/downloads/**

網頁中間有一個大大的黃色按鈕，寫著 `Download Python 3.x.x`
（版本號會隨時間變動，抓最新的就好）。

點下去，會下載一個約 30 MB 的檔案，檔名長這樣：

```
python-3.x.x-amd64.exe
```

## 1-3 執行安裝

打開剛才下載的 `.exe` 檔，會跳出安裝視窗。**請照這個順序做：**

1. 先看**視窗最下方**，有兩個小勾選框
2. **勾選 `Add python.exe to PATH`** —— 舊版本寫的是 `Add Python 3.x to PATH`，是同一個東西
3. 確認勾好之後，才點中間的 `Install Now`
4. 等待 1 到 2 分鐘，過程中可能會跳出使用者帳戶控制，按「是」
5. 看到 `Setup was successful` 就完成了
6. 如果畫面出現 `Disable path length limit` 這個選項，**點一下它**（可以避免之後路徑太長的問題），然後按 `Close`

> ⚠️ **第 2 步是整個安裝過程最重要的一步。**
> `Add python.exe to PATH` 的意思是「讓命令列找得到 Python」。
> 沒勾這個框，Python 雖然裝好了，但你在命令列輸入 `python` 會顯示「不是內部或外部命令」，
> 後面每一個步驟都會失敗。
>
> 已經裝好才發現忘記勾的話：重新執行同一個安裝檔，
> 選 `Modify`，一路下一步把 PATH 那個選項打開即可，不用移除重裝。

## 1-4 確認命令列可以叫得動 Python

**先把所有已經開著的命令列視窗全部關掉，再重新開一個。**

這一步不能跳過 —— PATH 設定只有在新開的視窗才會生效，
在舊視窗裡輸入指令會一直失敗，很多人卡在這裡。

重新開一個命令列（`Win` + `R` → `cmd` → Enter），輸入：

```
python --version
```

看到版本號就成功了：

```
Python 3.13.1
```

再確認套件管理工具 `pip` 也可以用（步驟 4 要靠它裝東西）：

```
python -m pip --version
```

看到類似這樣就對了：

```
pip 25.2 from C:\Users\你的名字\AppData\Local\Programs\Python\Python313\lib\site-packages\pip (python 3.13)
```

## 1-5 這一步的常見問題

| 症狀 | 原因與處理 |
|---|---|
| `'python' 不是內部或外部命令` | 安裝時沒勾 `Add python.exe to PATH`。重跑安裝檔選 `Modify` 把它打開，然後**重開命令列** |
| 輸入 `python` 卻跳出 Microsoft Store | Windows 內建的假捷徑在擋。到「設定 → 應用程式 → 進階應用程式設定 → 應用程式執行別名」，把兩個 `python.exe` / `python3.exe` 關掉 |
| 明明裝好了還是說找不到 | 命令列視窗是安裝前就開著的。關掉重開 |

---

# 步驟 2 ・ 把教材資料夾放好

## 2-1 放到桌面

把拿到的 `jupyter_MVP` 整個資料夾，複製到**桌面**（或任何你找得到的位置）。

> 路徑裡有中文沒關係，但盡量**不要放在有空格的深層路徑**，
> 也不要留在壓縮檔裡 —— 要先完整解壓縮出來。

## 2-2 讓命令列進到這個資料夾

最快的方法：

1. 用檔案總管打開 `jupyter_MVP` 資料夾
2. 點一下最上方的**路徑列**（顯示資料夾路徑的那一條）
3. 把路徑整個刪掉，直接輸入 `cmd`，按 Enter

命令列就會直接開在這個資料夾裡。

或者手動輸入（把路徑換成你實際放的位置）：

```
cd C:\Users\你的名字\Desktop\jupyter_MVP
```

`cd` 是 change directory 的縮寫，意思是「切換到某個資料夾」。
命令列永遠站在某一個資料夾裡，後面的指令都是對「現在站的地方」執行的。

## 2-3 確認位置正確

輸入：

```
dir
```

`dir` 會列出目前資料夾裡的所有檔案和資料夾。

清單裡應該要看得到這三個資料夾：`notebooks`、`movieapp`、`server`。

看不到的話表示位置不對，回到 2-2 重來。

---

# 步驟 3 ・ 建立虛擬環境

這一步只有兩行指令，但背後的觀念很重要，值得花兩分鐘看懂。

## 3-1 為什麼需要虛擬環境

假設你的電腦上有兩個 Python 專案：

- **專案 A** 用的是 Django 4.2
- **專案 B** 需要 Django 5.0

如果所有套件都裝在同一個地方，那麼裝了 5.0 就會蓋掉 4.2 ——
專案 B 能跑了，專案 A 反而壞掉。反過來也一樣。
你會陷入「修好一個、弄壞另一個」的循環。

這個狀況有個專有名詞叫 **相依性地獄（dependency hell）**，
是實務上非常常見的災難。

**虛擬環境的解法很單純：幫每一個專案準備一份獨立的套件資料夾。**

專案 A 的 Django 4.2 放在 A 自己的資料夾，
專案 B 的 Django 5.0 放在 B 自己的資料夾，兩邊完全不會碰到對方。

### 具體來說，`venv` 資料夾裡是什麼

執行 `python -m venv venv` 之後，資料夾裡會長出這些東西：

```
jupyter_MVP\
└── venv\
    ├── Scripts\              放「可以執行的指令」
    │   ├── activate           啟用用的指令碼
    │   ├── python.exe         這個環境專屬的 Python
    │   ├── pip.exe            這個環境專屬的 pip
    │   └── jupyter.exe        步驟 4 裝完才會出現
    ├── Lib\site-packages\    所有裝進來的套件都放這裡
    └── pyvenv.cfg             設定檔
```

**重點在 `Lib\site-packages\`。** 步驟 4 那行 `pip install` 裝的東西
全部落在這個資料夾裡，不會碰到 Windows 裡原本那個 Python。

以這堂課來說，裝完之後裡面會有**一百多個套件、兩萬多個檔案、大約 400 MB**
（Jupyter 的相依套件很多）。這些全部集中在 `venv\` 這一個資料夾裡 ——
所以課程結束後直接刪掉它，電腦就真的完全恢復原狀。

### 「啟用」到底改了什麼

`venv\Scripts\activate` **只做一件事：改變「你打 `python` 時電腦去哪裡找它」。**

| | 你打 `python` 時，實際執行的是 |
|---|---|
| 啟用前 | `C:\Users\你的名字\AppData\Local\Programs\Python\Python313\python.exe` |
| 啟用後 | `C:\Users\你的名字\Desktop\jupyter_MVP\venv\Scripts\python.exe` |

`pip` 和 `jupyter` 也一樣。所以**啟用之後裝的套件才會進到 `venv` 裡，
沒啟用就會裝到系統那個 Python** —— 這正是最常見的
`ModuleNotFoundError` 來源。

想自己確認的話，可以隨時輸入這行看現在用的是哪一個：

```
where python
```

啟用後第一行應該是 `...\jupyter_MVP\venv\Scripts\python.exe`。

## 3-2 對你的三個實際好處

1. **不會弄壞電腦上其他的 Python 程式** —— 這堂課裝什麼都影響不到別的地方
2. **想清掉很容易** —— 課程結束後直接刪掉 `venv` 資料夾，電腦就恢復原狀，不留任何痕跡
3. **環境可以複製** —— 換一台電腦時，靠 `requirements.txt` 就能重建出一模一樣的環境

## 3-3 建立

確認命令列現在在 `jupyter_MVP` 資料夾裡，輸入：

```
python -m venv venv
```

跑個幾秒鐘，沒有任何輸出就是成功了。
資料夾裡會多出一個 `venv` 資料夾 —— 那就是這個專案專屬的工具箱。

> 指令裡有兩個 `venv`：前面的是 Python 內建的虛擬環境工具，
> 後面的是你要建立的資料夾名稱。取名叫 `venv` 是慣例。

## 3-4 啟用

建立完還要「啟用」，命令列才會改用這個工具箱：

```
venv\Scripts\activate
```

## 3-5 怎麼確認啟用成功

**命令列的開頭會多出 `(venv)` 這個標記：**

```
(venv) C:\Users\你的名字\Desktop\jupyter_MVP>
```

看到 `(venv)` 就是成功了。這是判斷「現在在不在虛擬環境裡」唯一可靠的方法，
之後每次操作前都可以瞄一眼確認。

## 3-6 每次上課都要重新啟用

**關掉命令列視窗，`(venv)` 就消失了。** 虛擬環境的啟用只對當前這個視窗有效。

下次上課，一定要先 `cd` 到資料夾，再執行一次啟用指令，然後才啟動 Jupyter。

忘記啟用最典型的症狀，是明明裝過套件卻出現這個錯誤：

```
ModuleNotFoundError: No module named 'django'
```

看到它先檢查開頭有沒有 `(venv)`。

## 3-7 離開與清除

暫時離開虛擬環境（回到系統預設的 Python）：

```
deactivate
```

想完全清掉這堂課裝的所有東西：**直接把 `venv` 資料夾刪掉就好**，
不需要一個一個移除套件，也不會影響電腦上其他東西。

## 3-8 如果用 PowerShell 出現紅字

在 PowerShell 執行啟用指令時，可能出現：

```
因為這個系統上已停用指令碼執行，所以無法載入檔案 ...
```

這是 Windows 的安全機制。兩個解法擇一：

- **改用 `cmd`**（最簡單）—— 按 `Win` + `R` 輸入 `cmd`
- 或在 PowerShell 執行這行後再啟用一次：

```
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

> **這行在做什麼？** PowerShell 預設不准執行指令碼檔案，
> 而 `activate` 剛好就是一個指令碼。這行是暫時把這道限制放寬。
>
> **`-Scope Process` 是關鍵**：只對**現在這個視窗**有效。
> 關掉視窗就恢復原狀，不會改到系統設定，也不會影響其他程式。
> 下次開新視窗要再打一次。

---

# 步驟 4 ・ 安裝套件

**先確認命令列開頭有 `(venv)`**，然後輸入（**這是一整行**）：

```
python -m pip install jupyter requests pandas "Django>=4.2,<5.1"
```

### 這行指令拆開來看

| 這一段 | 意思 |
|---|---|
| `python -m pip` | 用**這個** Python 的 pip |
| `install` | 下載並安裝 |
| 後面那一串 | 要裝的東西 |

> **為什麼是 `python -m pip` 而不是直接打 `pip`？**
> 大部分時候兩者一樣。但電腦上有多個 Python 時（很常見），
> 直接打 `pip` 可能會裝到別的環境去 —— 裝好了卻 `import` 不到，
> 而且完全看不出原因。加上 `python -m` 就能確定「裝到現在這個環境」。

### 會裝進來什麼

**Jupyter** —— 這堂課的教材是 `.ipynb` 格式，要靠 Jupyter 才能打開、
才能一格一格執行程式碼。它是這堂課的主要操作介面。

**另外三個是課程會用到的工具。** 下面用這堂課實際的例子說明它們各自做什麼：

### `requests` —— 幫 Python 上網

你用瀏覽器打開一個網址，會看到網頁。`requests` 做的是同一件事，
但**不開視窗，直接把資料拿回程式裡**。

第 1 章你會用它去要秀泰影城的片單，一行就拿到現在上映什麼：

```
resp = requests.get("https://capi.showtimes.com.tw/4/app/bootstrap")
```

沒有它，Python 沒辦法跟外面的世界說話。這堂課要抓影城、
要查電影資料庫、要問 AI，全都靠它。

### `pandas` —— 把資料變成表格

程式抓回來的電影資料，原本長這樣（一層包一層，看不出重點）：

```
{"title": "希望：末日血戰", "meta": {"adult": false, "genre_ids": [878, 9648, 28],
 "id": 1058424, "original_language": "ko", "vote_average": 8.3, ...
```

第 3 章用 `pandas` 把它攤平成一列一部電影：

```
      片名       上映日期    評分       類型
 希望：末日血戰 2026-07-15  8.30  科幻、懸疑、動作
    驀然回首   2024-06-28  8.20     動畫、劇情
 蜘蛛人：重生日 2026-07-29  7.90  科幻、動作、冒險
```

**變成表格之後，一行就能做到原本要寫迴圈的事** ——
例如「依評分由高到低排序」：

```
df.sort_values("評分", ascending=False)
```

如果你用過 Excel 的排序和篩選，`pandas` 就是那些功能的程式版。

### `Django` —— 把程式變成一個網站

到第 5 章之前，你寫的東西都只能在 Jupyter 裡看。`Django` 負責
**把它變成一個真的網站**：別人打開瀏覽器輸入網址，就看得到畫面。

它做兩件事：

1. **接收網址、回傳資料** —— 例如有人開 `/api/showtimes/`，
   就去呼叫你第 1 章寫的函式，把片單包成 JSON 回去
2. **把網頁送出去** —— 有人開 `/`，就回傳那個電影清單頁面

這堂課的 API 每一支只有三到五行，因為真正的邏輯都在你前四章寫的
`movieapp/` 裡，Django 只負責「把網址接到函式上」。

### `"Django>=4.2,<5.1"` 那串符號是什麼

**`>=4.2,<5.1` 是版本範圍**：要 4.2 以上，但不能到 5.1。
其他三個沒寫版本，就是「抓最新的」。只有 Django 要綁版本，
是因為 5.1 之後改了一些東西，這份教材沒有跟上。

> ⚠️ **引號不能省。**
> `>` 和 `<` 在命令列裡有特殊意義（把輸出寫到檔案、從檔案讀入）。
> 沒加引號的話，命令列會這樣理解：
>
> ```
> 5.1: No such file or directory
> ```
>
> 然後資料夾裡莫名多出一個叫 `=4.2,` 的空檔案，**而 Django 根本沒裝到**。
> 加引號就是告訴命令列「這整串是一個名字，不要當符號看」。

> 為什麼不是 `pip install -r requirements.txt`？
> 因為這包教材**拿到手時只有 notebook**，`requirements.txt` 本身也是
> 第一本 notebook 寫出來的。你現在是從零開始，所以要把套件名稱直接列出來。
> 做完 `00_環境設定` 之後，資料夾裡就會有 `requirements.txt` 了，
> 換一台電腦時可以用它重建環境。

會花兩到三分鐘（Jupyter 的相依套件比較多）。最後看到這樣就成功了：

```
Successfully installed Django-5.0.14 jupyter-1.1.1 pandas-2.3.1 requests-2.32.5 ...
```

> 出現黃色的 `[notice] A new release of pip is available` 是正常提醒，不是錯誤，可以無視。

## 確認 Jupyter 裝好了

```
jupyter --version
```

會列出一串元件版本，有看到 `notebook` 這一項就對了。

---

# 步驟 5 ・ 啟動 Jupyter

## 5-1 先確認兩件事

啟動之前，命令列必須符合這兩個條件：

1. 開頭有 **`(venv)`** 標記
2. 目前位置在 **`jupyter_MVP`** 資料夾（不是 `notebooks` 或 `server`）

> ⚠️ **一定要先啟用虛擬環境再啟動 Jupyter。**
> Jupyter 會用「啟動它的那個 Python」當作執行核心（kernel）。
> 如果沒啟用就啟動，kernel 會是系統預設的 Python，
> 你在 notebook 裡 `import` 會全部失敗，而且錯誤訊息完全看不出原因。

## 5-2 啟動

```
jupyter notebook
```

命令列會印出一堆訊息，然後**瀏覽器會自動打開**一個檔案列表頁面。

沒有自動打開的話，在命令列的訊息裡找這樣一行網址，複製到瀏覽器：

```
http://localhost:8888/tree?token=a1b2c3d4e5f6...
```

> 網址後面那串 `token` 是一次性的密碼，每次啟動都不一樣，
> 用來確認只有你本人能存取，正常現象。

## 5-3 這個命令列視窗要一直開著

**這個黑色視窗就是 Jupyter 的本體。**
把它縮到工作列沒關係，但不要關掉 —— 關掉的話瀏覽器頁面就會失效。

接下來所有操作都在瀏覽器裡進行。

## 5-4 打開教材

在瀏覽器的檔案列表中：

1. 點進 `notebooks` 資料夾
2. 點 `00_環境設定.ipynb`

教材就會在新分頁打開。

## 5-5 關閉 Jupyter

用完之後，回到那個命令列視窗，按兩次 `Ctrl` + `C`
（第一次會問你確不確定，第二次確認關閉）。

或者在瀏覽器的 Jupyter 首頁點右上角 `Quit`。

---

# 步驟 6 ・ 依序做完六本教材

## 6-1 怎麼執行程式碼

Notebook 由一格一格的「儲存格（cell）」組成，有些是說明文字，有些是程式碼。

**執行方法：用滑鼠點一下那一格，然後按 `Shift` + `Enter`。**

- 執行中：格子左邊會顯示 `[*]`
- 執行完：變成數字，例如 `[1]`、`[2]`，代表這是第幾個被執行的
- 結果會直接顯示在那一格下面

**從上到下，一格一格依序執行。** 不要跳著點。

## 6-2 六本的順序與內容

| 順序 | 檔案 | 你會學到 | 產出 |
|---|---|---|---|
| 1 | `00_環境設定.ipynb` | 環境檢查、金鑰、統一的網路出入口 | `movieapp/config.py`、`http.py` |
| 2 | `01_影城API.ipynb` | 沒有文件的 API 怎麼逆向、髒資料怎麼清 | `movieapp/sources.py` |
| 3 | `02_TMDB.ipynb` | 正規 API、金鑰安全、為什麼不能只信第一筆、平行查詢 | `movieapp/tmdb.py` |
| 4 | `03_資料整合.ipynb` | 用穩定 id 跨來源去重、pandas 篩選分析 | `movieapp/merge.py` |
| 5 | `04_Gemini對話.ipynb` | 把資料交給 AI 回答（RAG 雛形）、多輪對話 | `movieapp/gemini.py` |
| 6 | `05_接成服務.ipynb` | 包成 API、寫出前端網頁、啟動伺服器 | `server/` 全部 |

## 6-3 為什麼一定要照順序

每一本都會用 `%%writefile` 這個指令，把你寫的程式碼**存成一個真正的檔案**。
後面的章節和最後的網頁服務，都要 `import` 這些檔案才能運作。

**這包教材拿到手時只有 notebook**，`movieapp/`、`server/` 兩個資料夾
都是你在課程中一格一格寫出來的 —— 包含最後那個網頁前端。

跳著做的話，會看到這樣的訊息：

```
缺少前置模組，這本 notebook 還不能跑：
  - movieapp/sources.py  ->  請先完整執行 01_影城API.ipynb
```

這不是壞掉，是教材在提醒你回去補做。照它說的那一本做完再回來就好。

## 6-4 第一本會做的事

`00_環境設定.ipynb` 裡面有一格會執行 `config.doctor()`，
印出一張完整的環境檢查表：

```
環境檢查
===============================================================
  [OK]  Python 版本              3.13.1（需要 3.9 以上）
  [OK]  requests 套件            已安裝
  [OK]  Django 套件              已安裝
  [OK]  pandas 套件              已安裝
  [OK]  movieapp/config.py       已產生
  [--]  movieapp/sources.py      尚未產生（由 01_影城API.ipynb 寫出）
  [--]  movieapp/tmdb.py         尚未產生（由 02_TMDB.ipynb 寫出）
  ...
  [OK]  TMDB 金鑰                已設定（eyJhbG…）
  [OK]  Gemini 金鑰              已設定（AQ.Ab8…）
===============================================================
結論：環境沒問題，可以開始。
```

> **那幾個標示「尚未產生」是正常的。**
> 它們要等你做完對應的章節才會出現 —— 這正是你接下來要做的事。
>
> 課堂上任何時候覺得環境怪怪的，都可以回來執行這一格，
> 八成的狀況它都能直接告訴你原因。

## 6-5 最後一本會把系統跑起來

`05_接成服務.ipynb` 會先把整個 Django 服務和前端網頁寫出來，
接著直接在 notebook 裡啟動伺服器，並用內嵌視窗把完整網頁顯示出來，
你不需要自己下任何指令。

**做完記得執行最後一格**，把伺服器關掉，
否則它會一直在背景佔用 8008 這個埠。

---

# 下次上課的時候

步驟 1 到 4 都只做一次。之後每次上課只要兩行：

```
cd C:\Users\你的名字\Desktop\jupyter_MVP
venv\Scripts\activate
```

確認開頭出現 `(venv)` 之後：

```
jupyter notebook
```

---

# 網路是必要的

**這套教材全程需要連線。** 影城、電影資料庫（TMDB）、AI（Gemini）
三個外部服務都是即時呼叫的，沒有離線備援。

上課前先做這件事：打開 `00_環境設定.ipynb`，跑到最後那一格連線測試，
確認四行都顯示「連得上」：

```
  秀泰影城      連得上
  美麗華影城    連得上
  TMDB         連得上
  Gemini       連得上
```

有任何一行「連不上」就先處理網路，不要往下做 —— 後面的章節會一路失敗。

> 教室網路如果會擋外部網站，請事先確認這四個網域通得過：
> `capi.showtimes.com.tw`、`www.miramarcinemas.tw`、
> `api.themoviedb.org`（含圖片的 `image.tmdb.org`）、
> `generativelanguage.googleapis.com`。

---

# 常見狀況

| 症狀 | 原因與處理 |
|---|---|
| `'python' 不是內部或外部命令` | Python 沒裝或沒加進 PATH。回步驟 1-5 那張表 |
| 輸入 `python` 跳出 Microsoft Store | Windows 的應用程式執行別名在擋。見步驟 1-5 |
| `'jupyter' 不是內部或外部命令` | 步驟 4 沒做完，或忘記啟用虛擬環境。檢查開頭有沒有 `(venv)` |
| notebook 裡 `ModuleNotFoundError` | 啟動 Jupyter 前忘了啟用虛擬環境。關掉 Jupyter，執行 `venv\Scripts\activate` 後重新啟動 |
| 在 notebook 裡裝套件沒效果 | 要用 `%pip install`，**不要用 `!pip install`** —— 後者會裝到別的地方 |
| 「缺少前置模組，這本 notebook 還不能跑」 | 跳著執行了。回去把訊息指定的那一本做完 |
| 格子左邊一直是 `[*]` 不動 | 那一格還在執行（例如正在抓網路資料）。等它跑完，真的卡住就從選單選 `Kernel → Restart` |
| 改了 `.py` 檔卻沒生效 | notebook 開頭的 `%autoreload 2` 沒執行到，重跑第一格 |
| `%%writefile` 說找不到路徑 | 該本 notebook 前面的「建立資料夾」那一格沒跑到，回去補跑 |
| `ModuleNotFoundError: movieapp` | `00_環境設定.ipynb` 還沒跑完，`movieapp/` 是它產生的 |
| 影城或 TMDB 連不上 | 檢查網路。這套教材沒有離線模式，見「網路是必要的」那一節 |
| 瀏覽器沒有自動打開 | 從命令列訊息裡複製 `http://localhost:8888/...` 那串網址 |
| `Error: That port is already in use` | 8008 被上次沒關掉的伺服器佔用。重跑 05 的啟動格，它會先關掉舊的 |
| AI 回「配額已用完（429）」 | 全班共用的免費額度暫時滿了。等幾分鐘，或改用自己的金鑰 |
| PowerShell 說「已停用指令碼執行」 | 改用 `cmd`，或見步驟 3-8 |
| 環境整個怪怪的 | 在 notebook 執行 `config.doctor()`，它會逐項告訴你哪裡有問題 |

---

# 附錄 A ・ 不用 Jupyter 直接啟動系統

這是**備用方案**，正常上課請走步驟 5、6。

如果你只是想快速看到成品長什麼樣（例如環境測試、或想先看全貌），
可以跳過 Jupyter，直接把系統跑起來。

**開一個新的命令列視窗**（跑著 Jupyter 的那個不能用），照順序打這五行：

```
cd C:\Users\你的名字\Desktop\jupyter_MVP
venv\Scripts\activate
python build_modules.py
cd server
python manage.py runserver 8008 --noreload
```

五行分別在做的事：

| 指令 | 做什麼 |
|---|---|
| `cd ...` | 切換到專案資料夾 |
| `venv\Scripts\activate` | 啟用虛擬環境，開頭要出現 `(venv)` |
| `python build_modules.py` | 從 notebook 裡把程式碼取出來，生成整套系統的檔案 |
| `cd server` | 再切進 `server` 子資料夾（下一行的程式在那裡） |
| `python manage.py runserver 8008 --noreload` | 啟動網頁伺服器 |

最後那行拆開來看：`manage.py` 是 Django 的管理程式，
`runserver` 是「把網站跑起來」，`8008` 是網址要用的埠號
（所以等一下開 `127.0.0.1:8008`），`--noreload` 是關掉自動重載
—— 不加的話 Django 會多開一個背景程序，在 Windows 上關不乾淨。

> ⚠️ **前兩行不能省。** 附錄 A 常常是新開一個視窗才翻到的，
> 那個視窗沒有 `(venv)`，直接跑第三行會出現
> `ModuleNotFoundError: No module named 'django'`。

看到這兩行才算成功：

```
Starting development server at http://127.0.0.1:8008/
Quit the server with CTRL-BREAK.
```

然後打開瀏覽器：**http://127.0.0.1:8008**

版面大約 0.1 秒就會出現，電影會在接下來幾秒內一批一批補上來 ——
它正在抓片單並逐部查評分海報，查到一部就顯示一部，不用等全部查完。
關閉系統：回到命令列按 `Ctrl` + `C`。

> `build_modules.py` 會直接從 notebook 檔案裡把程式碼取出來，
> **一次生出全部 19 個檔案**（`movieapp/`、`server/`、前端網頁都包含在內）。
> 它只是讀取檔案內容，不會執行 notebook，所以不需要 Jupyter。
>
> **但這樣你只會看到成品，看不到它是怎麼做出來的。**
> 課程的價值在於親手把這些程式碼寫出來，請務必回去走步驟 6。

---

# 附錄 B ・ 資料夾裡有什麼

```
jupyter_MVP/
├── notebooks/              課程本體，六本依序做
├── venv/                   步驟 3 建立的虛擬環境
├── .env                    你的金鑰（程式自動建立，不進版控）
├── build_modules.py        附錄 A 用的腳本
└── 安裝與啟動指南.docx      ← 你正在看的這份

     ↓ 以下全部是你做完課程後長出來的 ↓

├── movieapp/               系統核心邏輯
│   ├── config.py           金鑰、環境檢查、表格排版      ← 00
│   ├── http.py             所有對外請求的統一出入口      ← 00
│   ├── sources.py          兩家影城的片名               ← 01
│   ├── tmdb.py             評分、海報、類型             ← 02
│   ├── merge.py            合併去重、篩選               ← 03
│   └── gemini.py           AI 對話                     ← 04
├── server/                 網頁伺服器                   ← 05
│   ├── cinema/views.py     API 程式，每支只有三到五行
│   └── static/index.html   整個前端，就這一個檔案
├── requirements.txt                                    ← 00
└── .env.example                                        ← 00
```

**剛拿到教材時，`movieapp/` 和 `server/` 兩個資料夾還不存在。**
看到它們出現，就代表你的課程進度到哪了。

## 為什麼程式碼要存成 `.py` 而不是留在 cell 裡

notebook 儲存格裡的程式碼，**沒辦法被其他 notebook 或網頁服務使用**。

把邏輯集中在 `movieapp/`，notebook 和最後的網站就都只是它的使用者，
改一個地方兩邊同時生效，不會出現好幾份各自走鐘的複製品。

`server/cinema/views.py` 每支 API 只有三到五行，就是這個設計的證據。

> **要修改請回去改 notebook 的那一格再重新執行**，
> 不要直接編輯 `movieapp/*.py` —— 下次重跑那一格會蓋掉你手改的內容。

## 金鑰

課程會用到兩個外部服務：電影資料庫 **TMDB** 和 AI 服務 **Gemini**。
兩把金鑰都要自己申請，免費額度就夠上這門課。

| 服務 | 申請網址 | 要拿的東西 |
|---|---|---|
| TMDB | <https://www.themoviedb.org/settings/api> | API 讀取存取權杖（Read Access Token），**不是** API Key |
| Gemini | <https://aistudio.google.com/apikey> | Create API key |

**金鑰不在版本庫裡。** 第一次跑 `00_環境設定.ipynb` 或啟動伺服器時，
程式會在專案根目錄建一份空的 `.env`。填法兩種，寫的是同一個檔案：

1. **網頁上填**（推薦）：篩選列右邊的「API 金鑰」按鈕，貼上去存檔，
   伺服器立刻改用新的，不用重啟
2. **直接編輯 `.env`**：改完重跑 `00_環境設定.ipynb` 的開場，或重啟伺服器

還沒填的時候，網頁會自己跳出設定視窗，按鈕也會變成紅色的提醒。

> 建立 `.env` 的動作**絕不覆蓋既有的檔案**，重跑幾次都不會把金鑰清掉。

> **Gemini 的免費額度是按金鑰計算的。** 好幾個人共用一把時，
> 同時發問很容易一起撞到上限、收到 429。程式會把這個錯誤翻譯成
> 看得懂的中文說明，等幾分鐘或換自己的金鑰就好。

> **Windows 注意：** 檔案總管不讓你把檔案命名成 `.env`（點開頭的檔名
> 會被吃掉）。如果你手動建了一個叫 `env` 的檔案，程式是找不到的 ——
> 用 PowerShell 的 `Rename-Item env .env`，或讓程式自己建就好。

## 系統提供的網址

第六本把伺服器啟動後，這些網址可以直接用瀏覽器打開：

| 網址 | 內容 |
|---|---|
| `/` | 主畫面 |
| `/api/movies/` | **整合後的完整電影清單**（一次做完整條流程，第六本用它示範） |
| `/api/showtimes/` | 秀泰影城的片名清單 |
| `/api/miramar/` | 美麗華影城的片名清單 |
| `/api/tmdb/search/?query=片名` | 電影資料庫搜尋結果 |
| `/api/tmdb/genres/` | 電影類型對照表 |
| `/api/time/` | 伺服器時間 |
| `/api/keys/` | 查詢／設定 API 金鑰（只接受本機） |

（網址前面要加上 `http://127.0.0.1:8008`）

> 主畫面沒有用 `/api/movies/`，而是先跟 `/api/showtimes/`、`/api/miramar/`
> 要片單，再對每個片名打一次 `/api/tmdb/search/`。
> 這樣片單一到就能先畫出來，不必等到最後一部片查完才看得到東西。
